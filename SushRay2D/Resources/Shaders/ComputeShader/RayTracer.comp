#version 430 core

uniform float fperPix_X;
uniform float fperPix_Y;

layout(RGBA32F) writeonly uniform image2D staticLightMap;	//texture0
layout(local_size_x = 16, local_size_y = 16) in;

void main()
{
	ivec2 coord = ivec2(gl_LocalInvocationID.xy) + ivec2(gl_GlobalInvocationID.xy);
	imageStore(staticLightMap, coord, vec4(255, 1.0, 1.0, 1.0));
}